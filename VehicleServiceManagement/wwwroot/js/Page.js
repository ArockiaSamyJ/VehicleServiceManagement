$(document).ready(function () {        Pages = 1;        OffsetRows = 0;        showingentries = 0;        FetchCustomerDetails();        $('input').removeClass('form-styling');        $('input').addClass('form-control');});    var Pages;    var OffsetRows;    var showingentries;    function entrieschange() {        Pages = 1;        OffsetRows = 0;        showingentries = 0;        FetchCustomerDetails();    }   function page(e) {       mm = $(e).text();       Pages = parseInt(mm);       Entries = $('#entries').val();       if (Pages == 1) {           OffsetRows = 0;           showingentries = 0;       }       else {           OffsetRows = ((Entries * Pages) - Entries)           showingentries = ((Entries * Pages) - Entries);       }       FetchCustomerDetails();    }    var pageLen;    var curPage;    var item = [];    function pagelooping() {        var loop = Math.ceil(Count / Entries);        $('.page').remove();        var value = showingentries + 1;        var lastentries = (showingentries + parseInt(Entries));        if (lastentries > Count) {            lastentries = Count;        }        var show = "<label class='font-weight-bold rem'>Showing " + value + " to " + lastentries + " of " + Count + " Entries</label>"        $('.rem').remove();        $('.show').append(show);        pageLen = loop;        curPage = Pages;        item = [];        for (var i = 1; i <= pageLen; i++) {            item.push(i);        }        render(Pages, item, true);    }    var Count;    function isPageInRange(curPage, index, maxPages, pageBefore, pageAfter) {        if (index <= 1) {            // first 2 pages            return true;        }        if (index >= maxPages - 2) {            // last 2 pages            return true;        }        if (index >= curPage - pageBefore && index <= curPage + pageAfter) {            return true;        }    }    function render(curPage, item, first) {        //List value remove and show the dashboard        $('li.Value').remove();        var html = '', separatorAdded = false;        for (var i in item) {            if (isPageInRange(curPage, i, pageLen, 2, 2)) {                html += '<li data-page="' + i + '"class="Value" onclick="page(a' + item[i] + ')" id="a' + item[i] + '">' + item[i] + '</li>';                // as we added a page, we reset the separatorAdded                separatorAdded = false;            } else {                if (!separatorAdded) {                    // only add a separator when it wasn't added before                    html += '<li class="separator" />';                    separatorAdded = true;                }            }        }        var holder = document.querySelector('#holder');        holder.innerHTML = html;        document.querySelector('#holder>li[id="a' + curPage + '"]').classList.add('active');    }